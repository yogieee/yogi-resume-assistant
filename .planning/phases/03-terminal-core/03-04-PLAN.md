---
phase: 03-terminal-core
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - src/components/terminal/terminal-output.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "The terminal is visible in the right panel of the two-panel layout"
    - "On first load, the welcome message appears with guidance to type 'help'"
    - "Typing any of the 11 commands produces formatted output with real portfolio data"
    - "Typing an unrecognized command shows an error suggesting 'help'"
    - "The clear command resets the terminal to only the welcome message"
    - "The resume command triggers a PDF download"
    - "The terminal auto-scrolls to show new output"
    - "All output data comes from the shared portfolio data file"
  artifacts:
    - path: "src/components/terminal/terminal-output.tsx"
      provides: "Output dispatcher wired to all real renderers"
      min_lines: 40
    - path: "src/app/page.tsx"
      provides: "Page with TerminalShell in right panel"
  key_links:
    - from: "src/components/terminal/terminal-output.tsx"
      to: "src/components/terminal/renderers/*.tsx"
      via: "imports and renders all 11 renderer components in switch"
      pattern: "import.*Output.*from.*renderers"
    - from: "src/app/page.tsx"
      to: "src/components/terminal/terminal-shell.tsx"
      via: "renders TerminalShell in the right panel grid cell"
      pattern: "TerminalShell"
---

<objective>
Wire the terminal output dispatcher to real renderers and integrate the terminal shell into the page layout, replacing the placeholder terminal area.

Purpose: This is the integration plan that makes everything work end-to-end. After this plan, visitors can type commands and see real formatted portfolio content.
Output: Updated output dispatcher with real renderer imports, updated page.tsx with terminal shell.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-terminal-core/03-RESEARCH.md
@.planning/phases/03-terminal-core/03-02-SUMMARY.md
@.planning/phases/03-terminal-core/03-03-SUMMARY.md
@src/app/page.tsx
@src/components/terminal/terminal-output.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire output dispatcher to real renderers</name>
  <files>src/components/terminal/terminal-output.tsx</files>
  <action>
Update `src/components/terminal/terminal-output.tsx` to replace placeholder output with real renderer imports:

1. Import all 11 renderers from `./renderers/`:
   - `HelpOutput` from `./renderers/help-output`
   - `AboutOutput` from `./renderers/about-output`
   - `SkillsOutput` from `./renderers/skills-output`
   - `ExperienceOutput` from `./renderers/experience-output`
   - `ProjectsOutput` from `./renderers/projects-output`
   - `ArchitectureOutput` from `./renderers/architecture-output`
   - `AchievementsOutput` from `./renderers/achievements-output`
   - `CertificationsOutput` from `./renderers/certifications-output`
   - `ContactOutput` from `./renderers/contact-output`
   - `ResumeOutput` from `./renderers/resume-output`
   - `WelcomeOutput` from `./renderers/welcome-output`

2. Update the `renderOutput` switch statement to return real components:
   ```
   case "welcome": return <WelcomeOutput />
   case "help": return <HelpOutput />
   case "about": return <AboutOutput />
   case "skills": return <SkillsOutput />
   case "experience": return <ExperienceOutput />
   case "projects": return <ProjectsOutput />
   case "architecture": return <ArchitectureOutput />
   case "achievements": return <AchievementsOutput />
   case "certifications": return <CertificationsOutput />
   case "contact": return <ContactOutput />
   case "resume": return <ResumeOutput />
   case "error": return <ErrorOutput input={output.input} />
   default: return assertNever(output)
   ```

3. Keep the `ErrorOutput` inline in this file (it's simple: one div with error message).

4. Keep the command echo rendering (the `> command` line above output).
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify all 11 renderer imports resolve.</verify>
  <done>Output dispatcher routes all 13 CommandOutput types (11 commands + welcome + error) to their real renderer components. Exhaustiveness check remains.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate terminal into page layout</name>
  <files>src/app/page.tsx</files>
  <action>
Update `src/app/page.tsx` to replace the placeholder terminal area with the real `TerminalShell`:

1. Import `TerminalShell` from `@/components/terminal/terminal-shell`

2. Replace the entire right panel placeholder (the `<div className="flex flex-col min-h-[300px] bg-black">` block containing the "SYSTEM_READY: PHASE_3_DEPLOYED" text) with:
   ```tsx
   <div className="flex flex-col min-h-[300px]">
     <TerminalShell />
   </div>
   ```

3. Remove the old placeholder content (the centered text, dots animation).

4. Keep everything else in page.tsx unchanged: header, left panel (BadgeLoader), footer (ProfileActions), grid layout.

5. Ensure the terminal fills the right panel: the `TerminalShell` component already has `h-full` and `flex flex-col`, so it should fill the grid cell. If needed, add `h-full` to the wrapper div.
  </action>
  <verify>
Run `npm run dev` and verify in the browser:
1. Terminal appears in the right panel with "Yogi Dev Console v1.0" header
2. Welcome message is displayed on first load
3. Type `help` -- all 11 commands listed
4. Type `about` -- professional summary displayed
5. Type `skills` -- skills grouped by category
6. Type `experience` -- career timeline shown
7. Type `projects` -- Autowire.ai showcase displayed
8. Type `architecture` -- architecture diagram rendered
9. Type `achievements` -- achievements listed
10. Type `certifications` -- certs with status shown
11. Type `contact` -- clickable links displayed
12. Type `resume` -- download triggered, confirmation shown
13. Type `clear` -- terminal resets to welcome message
14. Type `asdfgh` -- error message suggesting 'help'
15. Terminal auto-scrolls on new output
16. Clicking terminal area focuses the input
  </verify>
  <done>The terminal is integrated into the page layout. All 11 commands work end-to-end with real formatted output from portfolio data. Welcome message shows on load. Error handling works for unknown commands. Clear resets state. Resume triggers download. Auto-scroll and focus management function correctly.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Terminal visible in right panel of two-panel layout
- All 11 commands produce formatted output with colored labels (OUT-01 through OUT-11)
- Output uses clickable links for contact and project URLs (OUT-12)
- Welcome message displays on first load (TERM-07)
- Terminal header shows "Yogi Dev Console v1.0" (TERM-01)
- Input has `>` prompt (TERM-02)
- All 11 commands parsed correctly (TERM-03)
- Output area scrolls with auto-scroll (TERM-04)
- Unknown commands show error with suggestion (TERM-03)
- All command data sourced from `@/data/portfolio` (PROD-03)
</verification>

<success_criteria>
The terminal is the primary interaction model of the portfolio. Visitors can type any of 11 commands and receive fully formatted, colored, portfolio-data-driven output. The complete Phase 3 success criteria from the roadmap are met: header, input, welcome message, all commands, auto-scroll, clear, resume download, error messages, and shared data source.
</success_criteria>

<output>
After completion, create `.planning/phases/03-terminal-core/03-04-SUMMARY.md`
</output>
