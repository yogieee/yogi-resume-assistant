---
phase: 03-terminal-core
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/terminal/renderers/help-output.tsx
  - src/components/terminal/renderers/about-output.tsx
  - src/components/terminal/renderers/skills-output.tsx
  - src/components/terminal/renderers/experience-output.tsx
  - src/components/terminal/renderers/projects-output.tsx
  - src/components/terminal/renderers/architecture-output.tsx
  - src/components/terminal/renderers/achievements-output.tsx
  - src/components/terminal/renderers/certifications-output.tsx
  - src/components/terminal/renderers/contact-output.tsx
  - src/components/terminal/renderers/resume-output.tsx
  - src/components/terminal/renderers/welcome-output.tsx
autonomous: true

must_haves:
  truths:
    - "Each command produces formatted output with colored labels using console theme tokens"
    - "All renderers import data from @/data/portfolio (not hardcoded)"
    - "Skills are grouped by category with colored category names"
    - "Experience shows company, role, dates, and bullet-point contributions"
    - "Projects show description, features, tech stack, architecture, and clickable live link"
    - "Contact shows clickable links for email, LinkedIn, GitHub, phone"
    - "Help lists all 11 commands with descriptions"
    - "Welcome message guides first-time visitors"
  artifacts:
    - path: "src/components/terminal/renderers/help-output.tsx"
      provides: "Help command listing all commands"
      exports: ["HelpOutput"]
    - path: "src/components/terminal/renderers/about-output.tsx"
      provides: "About command with name, role, summary"
      exports: ["AboutOutput"]
    - path: "src/components/terminal/renderers/skills-output.tsx"
      provides: "Skills grouped by category"
      exports: ["SkillsOutput"]
    - path: "src/components/terminal/renderers/experience-output.tsx"
      provides: "Career timeline with contributions"
      exports: ["ExperienceOutput"]
    - path: "src/components/terminal/renderers/projects-output.tsx"
      provides: "Project showcase with features, stack, link"
      exports: ["ProjectsOutput"]
    - path: "src/components/terminal/renderers/architecture-output.tsx"
      provides: "ASCII architecture diagram"
      exports: ["ArchitectureOutput"]
    - path: "src/components/terminal/renderers/achievements-output.tsx"
      provides: "Achievement list with metrics"
      exports: ["AchievementsOutput"]
    - path: "src/components/terminal/renderers/certifications-output.tsx"
      provides: "Certification list with status"
      exports: ["CertificationsOutput"]
    - path: "src/components/terminal/renderers/contact-output.tsx"
      provides: "Contact info with clickable links"
      exports: ["ContactOutput"]
    - path: "src/components/terminal/renderers/resume-output.tsx"
      provides: "Resume download confirmation message"
      exports: ["ResumeOutput"]
    - path: "src/components/terminal/renderers/welcome-output.tsx"
      provides: "Welcome message with onboarding guidance"
      exports: ["WelcomeOutput"]
  key_links:
    - from: "src/components/terminal/renderers/*.tsx"
      to: "src/data/portfolio.ts"
      via: "import { portfolio } from '@/data/portfolio'"
      pattern: "import.*portfolio.*from.*@/data/portfolio"
    - from: "src/components/terminal/renderers/help-output.tsx"
      to: "src/lib/terminal/commands.ts"
      via: "import COMMANDS to list command descriptions"
      pattern: "import.*COMMANDS.*from.*commands"
---

<objective>
Create all 11 command output renderers plus the welcome message renderer. Each renderer imports data from the shared portfolio file and formats it with the console theme tokens.

Purpose: These renderers are the "content" of the terminal -- what visitors actually see when they type commands. Every renderer must pull from portfolio data (PROD-03 requirement) and use consistent formatting with colored labels.
Output: 11 renderer components in `src/components/terminal/renderers/` covering all commands.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-terminal-core/03-RESEARCH.md
@.planning/phases/03-terminal-core/03-01-SUMMARY.md
@src/types/portfolio.ts
@src/data/portfolio.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Help, welcome, about, skills, and contact renderers</name>
  <files>
    src/components/terminal/renderers/help-output.tsx,
    src/components/terminal/renderers/welcome-output.tsx,
    src/components/terminal/renderers/about-output.tsx,
    src/components/terminal/renderers/skills-output.tsx,
    src/components/terminal/renderers/contact-output.tsx
  </files>
  <action>
All renderers follow a consistent pattern:
- Import `portfolio` from `@/data/portfolio` (where needed)
- Use Tailwind console theme classes: `text-glow-green` for headings/accents, `text-glow-cyan` for labels/categories, `text-glow-amber` for links/highlights, `text-console-text` for body text, `text-console-text-dim` for secondary text
- Each renderer returns a `<div className="space-y-2 mt-2">` wrapper
- Section headers: `<div className="text-glow-green font-bold">{Title}</div>`

**welcome-output.tsx:**
- Title: "Welcome to Yogi Dev Console v1.0"
- Show a brief tagline: "Interactive terminal portfolio of {portfolio.about.name}"
- Show guidance: "Type 'help' to see available commands"
- Use a border-left accent: `border-l-2 border-glow-green pl-3`
- Keep it compact (5-6 lines of output)

**help-output.tsx:**
- Import `COMMANDS` from `@/lib/terminal/commands`
- Title: "Available Commands"
- List each command with its description in a grid/table format:
  - Command name in `text-glow-cyan` with fixed width
  - Description in `text-console-text-dim`
- Use consistent spacing (e.g., `grid grid-cols-[120px_1fr] gap-y-1`)

**about-output.tsx:**
- Import portfolio.about
- Title: "About"
- Display: name (`text-glow-cyan`), role, location
- Display summary paragraph in `text-console-text`

**skills-output.tsx:**
- Import portfolio.skills
- Title: "Technical Skills"
- For each skill category: category name in `text-glow-cyan` followed by colon, skills joined with ` · ` separator in `text-console-text`
- Follows the exact pattern from research code example

**contact-output.tsx:**
- Import portfolio.contact
- Title: "Contact Information"
- For each contact item: label in `text-glow-cyan` (fixed width ~20 chars), value as clickable `<a>` link in `text-glow-amber hover:underline`
- Use `target="_blank" rel="noopener noreferrer"` on all links

Export each as a named export (e.g., `export function HelpOutput()`).
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors in renderer files.</verify>
  <done>Five renderers created: welcome (guidance message), help (command listing from COMMANDS), about (profile summary from portfolio.about), skills (categorized list from portfolio.skills), contact (clickable links from portfolio.contact). All use portfolio data, not hardcoded strings.</done>
</task>

<task type="auto">
  <name>Task 2: Experience, projects, architecture, achievements, certifications, resume renderers</name>
  <files>
    src/components/terminal/renderers/experience-output.tsx,
    src/components/terminal/renderers/projects-output.tsx,
    src/components/terminal/renderers/architecture-output.tsx,
    src/components/terminal/renderers/achievements-output.tsx,
    src/components/terminal/renderers/certifications-output.tsx,
    src/components/terminal/renderers/resume-output.tsx
  </files>
  <action>
Continue the same pattern as Task 1.

**experience-output.tsx:**
- Import portfolio.experience
- Title: "Experience"
- For each experience entry:
  - Role title in `text-glow-cyan font-bold`
  - Company + location in `text-console-text`, period in `text-console-text-dim`
  - Description in `text-console-text`
  - Contributions as bullet list with `text-console-text-dim` bullets (use ">" or "-" prefix in `text-glow-green`)
- Separate multiple entries with `border-t border-console-border pt-3 mt-3`

**projects-output.tsx:**
- Import portfolio.projects
- Title: "Projects"
- For each project:
  - Project name in `text-glow-cyan font-bold`, subtitle in `text-console-text-dim`
  - Description paragraph
  - "Features:" label in `text-glow-amber`, then bullet list of features
  - "Tech Stack:" label in `text-glow-amber`, then comma-joined tech items
  - Live URL as clickable link in `text-glow-amber hover:underline`

**architecture-output.tsx:**
- Import portfolio.projects (use `projects[0].architecture`)
- Title: "{project.name} Architecture"
- Render the architecture array as an ASCII-style flow diagram:
  - Use `whitespace-pre font-mono` container (Pitfall 6 from research)
  - Each architecture line is already in "A -> B" format in the data
  - Render each line with `text-glow-cyan` for the arrow/flow
  - Wrap in `overflow-x-auto` for narrow screens
- Add `text-console-text-dim text-xs` label: "System Architecture Diagram"

**achievements-output.tsx:**
- Import portfolio.achievements
- Title: "Key Achievements"
- Numbered or bulleted list of achievements
- Each achievement in `text-console-text` with a `text-glow-green` bullet/star prefix

**certifications-output.tsx:**
- Import portfolio.certifications
- Title: "Certifications"
- For each certification:
  - Name in `text-console-text`
  - Status badge: `completed` in `text-glow-green` with checkmark, `in-progress` in `text-glow-amber` with clock/spinner indicator
- Use simple text indicators (e.g., "[x]" for completed, "[~]" for in-progress) to match terminal aesthetic

**resume-output.tsx:**
- Simple confirmation message: "Downloading resume..." in `text-glow-green`
- Note: The actual download is triggered as a side effect in terminal-shell.tsx (Plan 02). This renderer just shows the confirmation text.

Export each as a named export.
  </action>
  <verify>Run `npx tsc --noEmit` -- no type errors. Verify all renderers import from `@/data/portfolio`: count of imports should match renderer count (all except help-output and resume-output import portfolio).</verify>
  <done>Six renderers created: experience (career timeline with contributions), projects (full showcase with features/stack/link), architecture (ASCII flow diagram), achievements (bulleted metrics), certifications (with status badges), resume (download confirmation). All pull from portfolio data.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- All 11 renderers exist in `src/components/terminal/renderers/`
- `grep -r "from.*@/data/portfolio" src/components/terminal/renderers/` shows imports in about, skills, experience, projects, architecture, achievements, certifications, contact renderers (8 of 11)
- help-output imports from `@/lib/terminal/commands` (for COMMANDS registry)
- welcome-output imports from `@/data/portfolio` (for name)
- resume-output has no portfolio import (just a confirmation string)
- No hardcoded portfolio data in any renderer (all from imports)
</verification>

<success_criteria>
All 11 command output renderers produce formatted, styled output using portfolio data. Every data-driven renderer imports from `@/data/portfolio`. Help imports from the command registry. The renderers are ready to be wired into the output dispatcher in Plan 04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-terminal-core/03-03-SUMMARY.md`
</output>
