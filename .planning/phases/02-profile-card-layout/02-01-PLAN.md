---
phase: 02-profile-card-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "On desktop (>=1024px), the page shows a two-panel layout with left sidebar area and right main area"
    - "On mobile (<1024px), the layout stacks vertically with profile area on top and terminal area below"
    - "The terminal area shows a styled placeholder matching the console theme"
    - "The motion package is installed and importable"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Two-panel responsive grid layout with glow backdrop and terminal placeholder"
      contains: "grid-cols-1 lg:grid-cols-"
    - path: "package.json"
      provides: "motion dependency"
      contains: "motion"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/globals.css"
      via: "console theme tokens"
      pattern: "console-(bg|surface|border|text)"
---

<objective>
Create the two-panel responsive grid layout and install the motion animation library.

Purpose: Establishes the page structure that the profile card and terminal will slot into. The left panel holds the profile card (Phase 2), the right panel holds the terminal (Phase 3). Also installs `motion` which is needed for profile card animations.

Output: Updated `page.tsx` with responsive grid layout, a subtle glow backdrop behind the left panel for glassmorphism visibility, and a styled terminal placeholder on the right.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-profile-card-layout/02-RESEARCH.md
@src/app/page.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion package</name>
  <files>package.json</files>
  <action>
Run `npm install motion` to add the motion animation library.

This is a locked prior decision: use `motion` (not `framer-motion`). The import path will be `from "motion/react"`.

Verify it installs successfully and appears in package.json dependencies.
  </action>
  <verify>Run `npm ls motion` and confirm it shows the installed version (11.x). Run `node -e "require('motion')"` to confirm the module resolves.</verify>
  <done>motion package appears in package.json dependencies and is importable</done>
</task>

<task type="auto">
  <name>Task 2: Create two-panel responsive grid layout with terminal placeholder</name>
  <files>src/app/page.tsx</files>
  <action>
Replace the current visual shell page with the production two-panel layout.

Layout structure:
- Outer `<main>` with `min-h-screen p-4 lg:p-8`
- Inner `<div>` with CSS Grid: `grid grid-cols-1 lg:grid-cols-[380px_1fr] gap-6 max-w-7xl mx-auto min-h-[calc(100vh-4rem)]`
- Left `<aside>` with `flex items-start lg:items-center justify-center` containing:
  - A `relative` wrapper div
  - A glow backdrop div (positioned absolute, behind content): `absolute inset-0 bg-gradient-to-br from-glow-cyan/10 via-transparent to-glow-purple/10 rounded-3xl blur-2xl` -- this gives the glassmorphism backdrop-blur something to work with (see Research Pitfall 1)
  - A placeholder comment `{/* ProfileCard goes here - Plan 02-03 */}` inside the relative wrapper
- Right `<section>` with `flex items-stretch` containing the terminal placeholder:
  - A div styled as empty terminal: `w-full min-h-[400px] lg:min-h-0 rounded-xl border border-console-border bg-console-surface flex items-center justify-center`
  - Inside, a dim text: `<p className="text-console-text-dim text-sm font-mono">Terminal v1.0 -- Phase 3</p>`

On mobile (<1024px): single column, profile area stacks on top, terminal below.
On desktop (>=1024px): profile card 380px left column, terminal fills remaining space.

Do NOT add `"use client"` -- this page is a server component. The ProfileCard client component will be imported in Plan 03.

Use existing console theme tokens from globals.css (console-bg, console-surface, console-border, console-text-dim, glow-cyan, glow-purple).
  </action>
  <verify>Run `npm run dev` and visually confirm:
1. On desktop viewport: two columns visible side by side
2. On narrow viewport (<1024px): single column stack
3. Terminal placeholder shows with console-surface background and border
4. A subtle cyan/purple gradient glow is visible behind the left panel area
5. Run `npx tsc --noEmit` to confirm no type errors</verify>
  <done>page.tsx renders a responsive two-panel grid with glow backdrop on the left and styled terminal placeholder on the right, using only existing console theme tokens</done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- `npx tsc --noEmit` passes with no type errors
- Desktop view shows two-panel layout (380px left, fluid right)
- Mobile view shows single-column stack
- Terminal placeholder is visible with console theme styling
- Glow gradient visible behind left panel area
</verification>

<success_criteria>
The page displays a responsive two-panel grid layout that stacks on mobile and shows side-by-side on desktop, with a subtle glow backdrop ready for glassmorphism and a styled terminal placeholder.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-card-layout/02-01-SUMMARY.md`
</output>
