---
phase: 02-profile-card-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/icons/linkedin-icon.tsx
  - src/components/icons/github-icon.tsx
  - src/components/social-links.tsx
  - public/resume.pdf
autonomous: true

must_haves:
  truths:
    - "LinkedIn and GitHub icons render as SVGs matching lucide-react stroke style"
    - "Social links component renders a row of icon buttons for all 4 contact types"
    - "Each social link opens the correct URL (mailto: for email, tel: for phone, external for LinkedIn/GitHub)"
    - "A resume PDF exists at /resume.pdf for download"
  artifacts:
    - path: "src/components/icons/linkedin-icon.tsx"
      provides: "LinkedIn brand SVG icon component"
      exports: ["LinkedinIcon"]
    - path: "src/components/icons/github-icon.tsx"
      provides: "GitHub brand SVG icon component"
      exports: ["GithubIcon"]
    - path: "src/components/social-links.tsx"
      provides: "Social links row component with icon mapping"
      exports: ["SocialLinks"]
    - path: "public/resume.pdf"
      provides: "Placeholder resume PDF for download"
  key_links:
    - from: "src/components/social-links.tsx"
      to: "src/types/portfolio.ts"
      via: "ContactInfo type import"
      pattern: "ContactInfo"
    - from: "src/components/social-links.tsx"
      to: "src/components/icons"
      via: "iconMap record mapping contact types to icon components"
      pattern: "iconMap"
---

<objective>
Create the social links component, custom brand icon SVGs, and resume PDF placeholder.

Purpose: These are reusable building blocks the profile card needs. The social links component maps contact data to clickable icon buttons. Custom SVG icons avoid lucide-react's deprecated brand icons. The resume PDF enables the download button.

Output: Social links component, LinkedIn/GitHub icon components, and a placeholder resume PDF in /public.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-profile-card-layout/02-RESEARCH.md
@src/types/portfolio.ts
@src/data/portfolio.ts
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand icon SVG components and placeholder resume PDF</name>
  <files>
    src/components/icons/linkedin-icon.tsx
    src/components/icons/github-icon.tsx
    public/resume.pdf
  </files>
  <action>
Create two custom SVG icon components to avoid lucide-react's deprecated brand icons (see Research: Pitfall 3).

**LinkedinIcon** (`src/components/icons/linkedin-icon.tsx`):
- Export a named function `LinkedinIcon` accepting `{ className?: string }`
- Render an SVG with viewBox="0 0 24 24", fill="none", stroke="currentColor", strokeWidth={2}, strokeLinecap="round", strokeLinejoin="round"
- Use the standard LinkedIn icon paths (same as lucide's deprecated icon):
  - Path: "M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"
  - Rect: width="4" height="12" x="2" y="9"
  - Circle: cx="4" cy="4" r="2"
- Apply `className` to the SVG element

**GithubIcon** (`src/components/icons/github-icon.tsx`):
- Export a named function `GithubIcon` accepting `{ className?: string }`
- Same SVG props as LinkedinIcon
- Use the standard GitHub icon path:
  - Path: "M15 22v-4a4.8 4.8 0 0 0-1-3.5c3 0 6-2 6-5.5.08-1.25-.27-2.48-1-3.5.28-1.15.28-2.35 0-3.5 0 0-1 0-3 1.5-2.64-.5-5.36-.5-8 0C6 2 5 2 5 2c-.3 1.15-.3 2.35 0 3.5A5.403 5.403 0 0 0 4 9c0 3.5 3 5.5 6 5.5-.39.49-.68 1.05-.85 1.65-.17.6-.22 1.23-.15 1.85v4"
  - Path: "M9 18c-4.51 2-5-2-7-2"

**Placeholder resume PDF** (`public/resume.pdf`):
- Create a minimal valid PDF file. Use a simple bash command to generate one:
  `echo '%PDF-1.0\n1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj\n2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj\n3 0 obj<</Type/Page/MediaBox[0 0 612 792]/Parent 2 0 R>>endobj\nxref\n0 4\n0000000000 65535 f \n0000000009 00000 n \n0000000058 00000 n \n0000000115 00000 n \ntrailer<</Size 4/Root 1 0 R>>\nstartxref\n190\n%%EOF' > public/resume.pdf`
- If generating a valid PDF is tricky, simply create an empty file and add a comment in the code noting it's a placeholder.

Neither icon component needs `"use client"` -- they are pure render functions with no hooks.
  </action>
  <verify>
- `npx tsc --noEmit` passes (icon components type-check)
- `ls public/resume.pdf` confirms file exists
- Icons import correctly: create a quick test by checking TypeScript can resolve both imports
  </verify>
  <done>LinkedinIcon and GithubIcon export SVG components matching lucide-react stroke style; placeholder resume.pdf exists in public/</done>
</task>

<task type="auto">
  <name>Task 2: Create SocialLinks component with icon mapping</name>
  <files>src/components/social-links.tsx</files>
  <action>
Create a social links component that maps ContactInfo[] to a row of clickable icon buttons.

**SocialLinks component** (`src/components/social-links.tsx`):
- This is a server component (no `"use client"` needed -- no hooks, no event handlers beyond native `<a>`)
- Accept props: `{ contacts: ContactInfo[] }` where ContactInfo is imported from `@/types/portfolio`
- Create an `iconMap` that maps ContactInfo["type"] to icon components:
  - `email` -> `Mail` from lucide-react (NOT deprecated)
  - `phone` -> `Phone` from lucide-react (NOT deprecated)
  - `linkedin` -> `LinkedinIcon` from `@/components/icons/linkedin-icon`
  - `github` -> `GithubIcon` from `@/components/icons/github-icon`
- Render a flex row (`flex items-center justify-center gap-3`)
- For each contact, render an `<a>` tag with:
  - `href={c.url}` (urls are already formatted: mailto:, tel:, https://)
  - `target="_blank" rel="noopener noreferrer"` for linkedin and github (external links)
  - No target/rel for email and phone (they use mailto: and tel: protocols)
  - `aria-label={c.label}` for accessibility
  - Styling: `p-2 rounded-lg text-console-text-dim hover:text-glow-cyan bg-console-elevated/50 hover:bg-console-elevated border border-transparent hover:border-console-border transition-colors duration-200`
  - Icon rendered inside at `className="size-4"`

Type the iconMap as `Record<ContactInfo["type"], React.ComponentType<{ className?: string }>>` for type safety.
  </action>
  <verify>
- `npx tsc --noEmit` passes with no errors
- The component correctly types the iconMap against all ContactInfo type values
  </verify>
  <done>SocialLinks component renders a row of 4 icon links (email, phone, linkedin, github) with correct URLs, accessibility labels, and console-themed hover states</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes for all new files
- `npm run dev` starts without errors
- LinkedinIcon and GithubIcon render SVGs with currentColor stroke
- SocialLinks accepts ContactInfo[] and renders all 4 contact types
- public/resume.pdf exists and is served at /resume.pdf
</verification>

<success_criteria>
All social link building blocks are ready: brand icons avoid deprecation warnings, social links component maps data to styled icon buttons, and resume PDF placeholder exists for download.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-card-layout/02-02-SUMMARY.md`
</output>
