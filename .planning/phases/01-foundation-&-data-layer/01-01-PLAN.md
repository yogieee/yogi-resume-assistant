---
phase: 01-foundation-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - components.json
  - public/resume.pdf
autonomous: true

must_haves:
  truths:
    - "Running npm run dev starts the Next.js application without errors"
    - "TypeScript compilation succeeds with no errors"
    - "shadcn/ui cn() utility is importable from @/lib/utils"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/app/layout.tsx"
      provides: "Root layout component"
      contains: "RootLayout"
    - path: "src/app/page.tsx"
      provides: "Home page component"
      contains: "export default"
    - path: "src/lib/utils.ts"
      provides: "cn() helper from shadcn/ui"
      contains: "cn"
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "shadcn"
    - path: "public/resume.pdf"
      provides: "Placeholder resume file"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Scaffold the Next.js project with TypeScript, Tailwind CSS v4, and shadcn/ui in the existing git repo.

Purpose: Create the runnable project foundation that all subsequent plans build on.
Output: A working Next.js app that starts with `npm run dev`, with shadcn/ui initialized and lucide-react installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-&-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project in existing repo</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, .gitignore</files>
  <action>
    The project root already has `.git` and `.planning` directories. Scaffold the Next.js project WITHOUT creating a new git repo.

    Option A (preferred): Run create-next-app directly in the project root:
    ```
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack --yes
    ```
    The `.` tells it to use the current directory. The `--yes` flag skips prompts.

    If Option A fails due to non-empty directory, use Option B:
    - Create in temp directory: `npx create-next-app@latest /tmp/yogi-scaffold --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --turbopack --yes`
    - Copy all files (except `.git`) from `/tmp/yogi-scaffold/` into the project root
    - Clean up temp directory

    After scaffold exists, verify `npm run dev` starts successfully (run briefly and kill).

    IMPORTANT: Do NOT create a `tailwind.config.js` or `tailwind.config.ts` file. Tailwind v4 uses CSS-based configuration via `@theme` in globals.css. If create-next-app generates one, delete it.
  </action>
  <verify>
    - `npm run dev` starts without errors (run for 5 seconds, then kill)
    - `ls src/app/layout.tsx src/app/page.tsx src/app/globals.css` -- all exist
    - `cat package.json | grep next` -- next.js is a dependency
    - No `tailwind.config.js` or `tailwind.config.ts` exists
  </verify>
  <done>Next.js project runs with `npm run dev`, App Router structure exists in src/app/</done>
</task>

<task type="auto">
  <name>Task 2: Initialize shadcn/ui and install supporting packages</name>
  <files>components.json, src/lib/utils.ts, src/app/globals.css, public/resume.pdf</files>
  <action>
    1. Initialize shadcn/ui with the latest CLI:
       ```
       npx shadcn@latest init --defaults
       ```
       If `--defaults` doesn't work or it prompts interactively, choose: New York style, Zinc base color. The CLI auto-detects Tailwind v4 and generates OKLCH color variables.

       After init, verify that `src/lib/utils.ts` exists and contains the `cn()` function (uses clsx + tailwind-merge).

    2. Install lucide-react for icons (used in later phases):
       ```
       npm install lucide-react
       ```

    3. Create placeholder resume PDF at `public/resume.pdf`. Create a minimal valid PDF:
       ```
       echo "%PDF-1.4 placeholder" > public/resume.pdf
       ```
       This ensures the file exists for the resume download feature in later phases. The user will replace it with their actual resume.

    4. Verify shadcn/ui generated compatible output:
       - `globals.css` should contain `@import "tw-animate-css"` (NOT `@plugin 'tailwindcss-animate'` which is the v3 pattern)
       - `components.json` should exist with shadcn configuration
       - `src/lib/utils.ts` should export `cn()`

    IMPORTANT: Do NOT modify `globals.css` theme variables yet -- that is Plan 03's job. Just verify shadcn/ui init produced valid Tailwind v4 output.
  </action>
  <verify>
    - `cat src/lib/utils.ts` contains `cn` function
    - `cat components.json` exists and contains valid JSON
    - `cat package.json | grep lucide-react` shows it's installed
    - `ls public/resume.pdf` exists
    - `npm run build` completes without errors
  </verify>
  <done>shadcn/ui is initialized with cn() utility available, lucide-react installed, placeholder resume exists, project builds cleanly</done>
</task>

</tasks>

<verification>
- `npm run dev` starts the dev server without errors
- `npm run build` completes without errors
- Project structure matches: src/app/(layout.tsx, page.tsx, globals.css), src/lib/utils.ts, components.json
- No tailwind.config.js or tailwind.config.ts exists (Tailwind v4 uses CSS config)
</verification>

<success_criteria>
A clean Next.js 16 project that builds and runs, with shadcn/ui initialized, lucide-react installed, and a placeholder resume PDF in public/. Ready for data layer (Plan 02) and theme customization (Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-&-data-layer/01-01-SUMMARY.md`
</output>
