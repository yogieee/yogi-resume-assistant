---
phase: 01-foundation-and-data-layer
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/types/portfolio.ts
  - src/data/portfolio.ts
autonomous: true

must_haves:
  truths:
    - "A single portfolio data file contains ALL of Yogi's professional data (about, skills, experience, projects, achievements, certifications, contact)"
    - "Portfolio data types are importable by any component without type errors"
    - "No section contains empty arrays or placeholder strings"
  artifacts:
    - path: "src/types/portfolio.ts"
      provides: "TypeScript interfaces for all portfolio sections"
      exports: ["AboutData", "SkillCategory", "Experience", "Project", "Achievement", "Certification", "ContactInfo", "PortfolioData"]
      min_lines: 40
    - path: "src/data/portfolio.ts"
      provides: "Complete portfolio data as typed constant"
      exports: ["portfolio"]
      min_lines: 100
  key_links:
    - from: "src/data/portfolio.ts"
      to: "src/types/portfolio.ts"
      via: "type import"
      pattern: "import.*type.*from.*@/types/portfolio"
    - from: "src/data/portfolio.ts"
      to: "PortfolioData"
      via: "satisfies or type annotation"
      pattern: "(satisfies PortfolioData|: PortfolioData)"
---

<objective>
Create the typed portfolio data layer -- TypeScript interfaces and a single data file containing ALL of Yogi's professional content.

Purpose: Establish the single source of truth (requirement PROD-03) that feeds the profile card, terminal commands, and AI context in all downstream phases.
Output: `src/types/portfolio.ts` (interfaces) and `src/data/portfolio.ts` (data).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-&-data-layer/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio TypeScript interfaces</name>
  <files>src/types/portfolio.ts</files>
  <action>
    Create `src/types/portfolio.ts` with comprehensive interfaces for ALL portfolio sections. Define these interfaces:

    - `AboutData` -- name, role, tagline, location, summary (all strings)
    - `Skill` -- name (string)
    - `SkillCategory` -- category (string), skills (Skill[])
    - `Experience` -- title, company, location, period, description (strings), contributions (string[])
    - `ProjectFeature` -- name (string)
    - `Project` -- name, subtitle, description (strings), features (ProjectFeature[]), architecture (string[]), techStack (string[]), liveUrl (string)
    - `Achievement` -- description (string)
    - `Certification` -- name (string), status ("completed" | "in-progress")
    - `ContactInfo` -- type ("email" | "linkedin" | "github" | "phone"), label, value, url (strings)
    - `PortfolioData` -- top-level interface combining all sections: about (AboutData), skills (SkillCategory[]), experience (Experience[]), projects (Project[]), achievements (Achievement[]), certifications (Certification[]), contact (ContactInfo[])

    Export ALL interfaces. Use `export interface` for each.

    Do NOT use `any`, `Record<string, unknown>`, or loose typing. Every field must be explicitly typed.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - File contains all 10 exported interfaces (AboutData, Skill, SkillCategory, Experience, ProjectFeature, Project, Achievement, Certification, ContactInfo, PortfolioData)
  </verify>
  <done>All portfolio interfaces are defined and exported, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Create complete portfolio data file</name>
  <files>src/data/portfolio.ts</files>
  <action>
    Create `src/data/portfolio.ts` importing the `PortfolioData` type and exporting a `portfolio` constant with ALL sections fully populated. Use `satisfies PortfolioData` for type safety with inference.

    ```typescript
    import type { PortfolioData } from "@/types/portfolio";
    export const portfolio = { ... } satisfies PortfolioData;
    ```

    Fill in ALL data from PROJECT.md context:

    **about:**
    - name: "Yogananda Govind"
    - role: "Senior Software Engineer | AI & Cloud Engineer"
    - tagline: "Building AI powered cloud platforms and automation systems"
    - location: "United Kingdom"
    - summary: "Full-stack engineer with 12+ years of experience building enterprise systems and cloud-native platforms. Currently focused on AI powered automation systems using AWS. Creator of Autowire.ai, an intelligent document processing SaaS platform built with serverless architecture."

    **skills** (6 categories):
    - Languages: Java, TypeScript, JavaScript, SQL
    - Frameworks: Next.js, React, Spring Boot, Node.js
    - Cloud (AWS): Lambda, API Gateway, S3, DynamoDB, Textract, Bedrock, CDK, CloudFormation, Cognito, SQS, SNS, EventBridge, CloudWatch, IAM
    - AI & Data: Amazon Bedrock, Claude API, Amazon Textract, LangChain
    - DevOps: Docker, GitHub Actions, AWS CDK, CloudFormation, Terraform
    - Databases: DynamoDB, PostgreSQL, Oracle, MySQL

    **experience** (at minimum the Mphasis role):
    - title: "Senior Software Engineer"
    - company: "Mphasis UK Limited"
    - location: "London, United Kingdom"
    - period: "2019 - Present"
    - description: "Enterprise welfare platform for UK Government using IBM Curam SPM"
    - contributions: ["Led development of enterprise welfare platform serving UK Government departments", "Designed and implemented complex business rules engine processing 10,000+ daily transactions", "Mentored team of 5 junior developers, establishing code review practices and CI/CD pipelines", "Reduced system processing time by 40% through database query optimization and caching strategies"]

    **projects** (Autowire.ai):
    - name: "Autowire.ai"
    - subtitle: "AI-Powered Document Extraction SaaS"
    - description: "Intelligent document processing platform that automates data extraction from complex documents using AI. Built as a multi-tenant SaaS with serverless architecture on AWS."
    - features: ["AI-powered document classification and data extraction", "Multi-tenant architecture with isolated data processing", "Real-time processing status tracking and notifications", "RESTful API with comprehensive SDK for integrations", "Role-based access control with AWS Cognito"]
    - architecture: ["Next.js Frontend → API Gateway → Lambda Functions", "S3 Document Storage → Textract Processing Pipeline", "Bedrock AI Enhancement → DynamoDB Results Store", "EventBridge Orchestration → SQS Queue Processing", "CloudWatch Monitoring → SNS Alert Notifications"]
    - techStack: ["Next.js", "TypeScript", "AWS Lambda", "API Gateway", "DynamoDB", "S3", "Textract", "Bedrock", "CDK", "Cognito", "EventBridge", "SQS", "SNS"]
    - liveUrl: "https://autowire.ai"

    **achievements:**
    - "Reduced document processing time by 90% through AI-powered automation pipeline"
    - "Achieved 40% cost optimization in AWS infrastructure through serverless architecture redesign"
    - "Built multi-tenant SaaS platform handling 1000+ documents daily across 50+ client organizations"
    - "Orchestrated 15+ Lambda microservices with event-driven architecture using EventBridge and SQS"
    - "Implemented zero-downtime deployment pipeline with AWS CDK and GitHub Actions"

    **certifications:**
    - AWS Solutions Architect Associate (completed)
    - AWS Generative AI Certification (in-progress)

    **contact:**
    - email: label "Email", value "Yoganand.Govind@gmail.com", url "mailto:Yoganand.Govind@gmail.com"
    - linkedin: label "LinkedIn", value "yoganandgovind", url "https://linkedin.com/in/yoganandgovind"
    - github: label "GitHub", value "yogieee", url "https://github.com/yogieee"
    - phone: label "Phone", value "+44 07365558999", url "tel:+4407365558999"

    CRITICAL: Every section must be fully populated. No empty arrays. No "TODO" or placeholder strings. This is the single source of truth for the entire application.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - Data file imports PortfolioData type (grep for "import.*PortfolioData")
    - Data file uses `satisfies PortfolioData` (grep for "satisfies")
    - All 7 sections populated: about, skills (6 categories), experience (1+), projects (1+), achievements (5+), certifications (2+), contact (4 entries)
    - No empty arrays: `grep "\[\]" src/data/portfolio.ts` returns nothing
  </verify>
  <done>Complete portfolio data exists as a typed, single source of truth with all 7 sections fully populated and type-checked</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes -- types and data are consistent
- `src/types/portfolio.ts` exports PortfolioData and all sub-interfaces
- `src/data/portfolio.ts` exports `portfolio` constant with all sections populated
- A quick import test: create a temp file that does `import { portfolio } from "@/data/portfolio"; console.log(portfolio.about.name)` and verify it compiles
</verification>

<success_criteria>
The portfolio data layer is complete: comprehensive TypeScript interfaces in `src/types/portfolio.ts` and a fully populated data file in `src/data/portfolio.ts`. Any component can `import { portfolio } from "@/data/portfolio"` and access typed data for all 7 sections without type errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-&-data-layer/01-02-SUMMARY.md`
</output>
