---
phase: 05-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/page.tsx
  - src/components/mode-toggle.tsx
  - src/components/ai/ai-shell.tsx
  - src/components/ai/ai-welcome.tsx
  - src/components/ai/ai-thinking.tsx
  - src/components/ai/ai-message.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "A visible toggle switches between AI Mode and Terminal Mode"
    - "AI mode is the default experience on page load"
    - "Switching modes shows an animated transition"
    - "AI mode displays a welcome block with conversation starters"
    - "Switching to terminal mode stops any active AI streaming"
  artifacts:
    - path: "src/components/mode-toggle.tsx"
      provides: "Toggle switch component between ai and terminal modes"
      exports: ["ModeToggle"]
    - path: "src/components/ai/ai-shell.tsx"
      provides: "AI chat container with useChat hook, message list, and input"
      exports: ["AiShell"]
    - path: "src/components/ai/ai-welcome.tsx"
      provides: "Welcome block with Yogi AI intro and conversation starters"
      exports: ["AiWelcome"]
    - path: "src/components/ai/ai-thinking.tsx"
      provides: "Animated thinking indicator"
      exports: ["AiThinking"]
    - path: "src/components/ai/ai-message.tsx"
      provides: "Single message renderer for user and assistant messages"
      exports: ["AiMessage"]
    - path: "src/app/page.tsx"
      provides: "Mode state management, conditional rendering of AI/Terminal"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ai/ai-shell.tsx"
      via: "conditional render based on mode state"
      pattern: "mode.*===.*ai.*AiShell"
    - from: "src/components/ai/ai-shell.tsx"
      to: "@ai-sdk/react useChat"
      via: "hook providing messages, sendMessage, status"
      pattern: "useChat"
    - from: "src/components/mode-toggle.tsx"
      to: "src/app/page.tsx"
      via: "onToggle callback updating mode state"
      pattern: "onToggle|setMode"
---

<objective>
Install AI SDK packages, create the mode toggle between AI and Terminal, build AI shell with useChat hook, and wire mode switching into the page layout.

Purpose: Establishes the AI-first experience with mode switching -- the foundational UI layer that all streaming and API work builds on.
Output: Working mode toggle, AI shell container with useChat wiring (will show errors until API route exists in Plan 02), welcome block, thinking indicator, message renderer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-integration/05-CONTEXT.md
@.planning/phases/05-ai-integration/05-RESEARCH.md
@src/app/page.tsx
@src/components/terminal/terminal-shell.tsx
@src/components/terminal/terminal-input.tsx
@src/data/portfolio.ts
@src/types/portfolio.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install AI SDK packages and create mode toggle + AI sub-components</name>
  <files>
    package.json
    src/components/mode-toggle.tsx
    src/components/ai/ai-welcome.tsx
    src/components/ai/ai-thinking.tsx
    src/components/ai/ai-message.tsx
  </files>
  <action>
1. Install packages:
   ```
   npm install ai @ai-sdk/anthropic @ai-sdk/react zod
   ```

2. Create `src/components/mode-toggle.tsx`:
   - Props: `mode: 'ai' | 'terminal'`, `onToggle: (mode: 'ai' | 'terminal') => void`
   - Visual design: a segmented toggle or switch styled to match the console theme
   - Labels: "Yogi AI" for AI mode, "Terminal" for terminal mode
   - Use existing console color variables (text-glow-green, text-console-text-dim, bg-console-surface, border-console-border)
   - Include a subtle animated transition indicator when switching (e.g., a brief "Switching to Developer Console..." or "Switching to Yogi AI..." text flash using motion from motion/react)
   - Mark as 'use client'

3. Create `src/components/ai/ai-welcome.tsx`:
   - Header: "Yogi AI -- Portfolio Assistant"
   - Subtitle: "Ask me anything about Yoganand's experience, projects, and skills"
   - 4-5 clickable conversation starter chips: "What are Yogi's key skills?", "Tell me about Autowire.ai", "What cloud technologies does he use?", "Why hire Yoganand?", "Start a project discussion"
   - Props: `onStarterClick: (text: string) => void` -- called when user clicks a starter
   - Style: console-themed, chips as bordered pills with hover glow effect
   - Mark as 'use client'

4. Create `src/components/ai/ai-thinking.tsx`:
   - Animated "Thinking..." text with pulsing dots
   - Use CSS animation (not JS interval) for the dot animation
   - Style: text-console-text-dim with subtle opacity animation
   - Props: none (pure presentational)

5. Create `src/components/ai/ai-message.tsx`:
   - Props: `message: UIMessage` (from 'ai' package)
   - Render user messages with "you >" prefix in glow-green
   - Render assistant messages with "yogi-ai >" prefix in a distinct accent color (e.g., cyan/blue from console palette)
   - Access text content via `message.parts` array -- iterate parts, render `part.type === 'text'` as text spans
   - Support basic markdown-like formatting in assistant messages: bold (**text**), bullet lists (- item), code blocks (backticks) -- use simple regex replacement or render as pre-formatted text with whitespace preserved
   - Do NOT use a heavy markdown library -- keep it lightweight with CSS white-space: pre-wrap and simple inline replacements
  </action>
  <verify>
    - `npm ls ai @ai-sdk/anthropic @ai-sdk/react zod` shows all four installed
    - All four component files exist and export their named components
    - No TypeScript errors: `npx tsc --noEmit` passes
  </verify>
  <done>AI SDK packages installed. ModeToggle, AiWelcome, AiThinking, and AiMessage components created with correct types and console styling.</done>
</task>

<task type="auto">
  <name>Task 2: Create AI shell container and wire mode switching into page</name>
  <files>
    src/components/ai/ai-shell.tsx
    src/app/page.tsx
  </files>
  <action>
1. Create `src/components/ai/ai-shell.tsx`:
   - Import `useChat` from `@ai-sdk/react`
   - Destructure: `messages`, `sendMessage`, `status`, `error`, `stop`, `setMessages`
   - Manage local input state with `useState` (AI SDK v6 does NOT provide input/handleInputChange -- use sendMessage with a manual text input)
   - Layout mirrors terminal-shell.tsx structure:
     - Window chrome bar with dots + "yogi@portfolio -- ai" label
     - Scrollable message area
     - Input bar at bottom with form submit
   - Show `<AiWelcome>` when `messages.length === 0` -- pass `onStarterClick` that calls `sendMessage({ role: 'user', parts: [{ type: 'text', text: starterText }] })` (UIMessage v6 format with parts array)
   - Map `messages` to `<AiMessage>` components
   - Show `<AiThinking>` when `status === 'submitted'`
   - Handle error state: when `error` is truthy, show a friendly in-persona message at the bottom of the message list: "I'm getting a lot of questions right now. Try again in a moment." styled as an assistant message
   - Input: form with text input, styled to match terminal-input (glow-green prompt, but use "ask >" instead of "yogi@portfolio %")
   - On form submit: call `sendMessage({ role: 'user', parts: [{ type: 'text', text: inputValue }] })`, clear local input state
   - Auto-scroll to bottom on new messages (same pattern as terminal-shell: useRef + scrollIntoView)
   - Accept `onStopStreaming` or expose `stop` -- will be called by parent on mode switch
   - Export a ref handle or accept a prop `active: boolean` -- when active becomes false, call `stop()` to abort streaming (use useEffect watching active prop)
   - Mark as 'use client'

2. Update `src/app/page.tsx`:
   - Convert to a client component ('use client') -- needed for useState mode management
   - Add state: `const [mode, setMode] = useState<'ai' | 'terminal'>('ai')` -- AI is default
   - Add `<ModeToggle>` in the terminal/AI panel header area (above the shell, inside the right panel)
   - Conditionally render `<AiShell active={mode === 'ai'} />` or `<TerminalShell />` based on mode
   - Keep the rest of the layout unchanged (header, 3D badge, footer)
   - The mode toggle should sit in the right panel's top area, between the panel top and the shell component
   - Import ModeToggle, AiShell, and keep TerminalShell import

IMPORTANT NOTES:
- Use `sendMessage` (NOT `handleSubmit` or `append`) -- this is AI SDK v6
- Messages use `parts` array (NOT `content` string) -- this is AI SDK v6
- Import `useChat` from `@ai-sdk/react` (NOT `ai/react`)
- The useChat hook will try to POST to `/api/chat` by default -- this route doesn't exist yet (Plan 02 creates it), so AI mode will show an error state if a message is sent before Plan 02 runs. This is expected.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors
    - `npm run build` completes (may have runtime errors without API route, but build should succeed)
    - Page loads at localhost:3000 showing AI mode by default with welcome block
    - Toggle switches between AI and Terminal views
    - Terminal mode still works fully (all commands functional)
  </verify>
  <done>AI shell container wired with useChat hook. Page defaults to AI mode with visible toggle. Mode switching works with animated transition. Terminal mode remains fully functional when selected.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts successfully
2. Page loads in AI mode by default showing "Yogi AI -- Portfolio Assistant" welcome with conversation starters
3. Toggle switches to Terminal mode -- terminal works as before (type "help", see output)
4. Toggle back to AI mode -- welcome block reappears
5. Build succeeds: `npm run build`
</verification>

<success_criteria>
- AI SDK packages (ai, @ai-sdk/anthropic, @ai-sdk/react, zod) installed
- Mode toggle visible and functional, switching between AI and Terminal views
- AI mode shows welcome block with conversation starters
- Terminal mode remains fully functional
- AI shell has input, message list, thinking indicator, and error display wired up
- Page defaults to AI mode on load
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-integration/05-01-SUMMARY.md`
</output>
